{
  "name": "godelivery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
